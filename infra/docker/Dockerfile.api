FROM python:3.12-slim

WORKDIR /app

COPY apps/api/pyproject.toml .
COPY apps/api/src ./src

RUN pip install --no-cache-dir -e .

EXPOSE 8000

CMD ["uvicorn", "xautopilot.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
